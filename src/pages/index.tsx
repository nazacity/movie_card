import { Box } from '@mui/system';
import React from 'react';

const SignIn = () => {
  return (
    <Box
      sx={{
        height: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    ></Box>
  );
};

export default SignIn;
